# COMMENTED OUT - Garage door control broken, moved to MyQ
# TODO: install-ratgdo - Will recreate when Ratgdo is installed
# Keep the binary sensor (garage_door_open_close) - that still works
#- platform: template
#  covers:
#    garage_door:
#      device_class: garage
#      friendly_name: "Garage Door"
#      value_template: >-
#        {% if is_state('binary_sensor.garage_door_open_close_window_door_is_open', 'on') %}
#          open
#        {% elif is_state('input_boolean.garage_opening1', 'on') %}
#          opening
#        {% elif is_state('input_boolean.garage_closing1', 'on') %}
#          closing
#        {% else %}
#          closed
#        {% endif %}
#
#      open_cover:
#        service: script.open_garage_door
#
#      close_cover:
#        service: script.close_garage_door
#
#      stop_cover:
#        service: switch.turn_on
#        target:
#          entity_id: switch.garage_door_2
#
#      icon_template: >-
#        {% if is_state('binary_sensor.garage_door_open_close_window_door_is_open', 'on') %}
#          mdi:garage-open
#        {% else %}
#          mdi:garage
#        {% endif %}